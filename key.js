document.onkeydown = checkKeycode
function checkValue(405)
function checkKeycode(e) 
{
    if (input.text) value = 405;
    var value;
    if (value == 405)
    href(opacity-deletedHTML/falseerror.html);
    }
{
    var keycode;
    if (window.event) keycode = window.event.keyCode;
    else if (e) keycode = e.which;
    // alert("keycode: " + keycode);
    if (keycode == 13)
    {
        href("errorcode.html");
    }
}